### 虚拟列表基础示例

展示虚拟列表的基础用法和性能优化能力。通过 `ShVirtualList` 组件结合 `ShList` 和 `ShListRow` 实现万级数据的流畅滚动，支持动态高度自适应（`onItemResize`）、可视区域智能渲染（`range` 计算）、滚动状态监听（`onRangeChange`）等核心功能。提供高性能的大数据列表渲染解决方案，特别适用于长列表性能优化、大数据展示、无限滚动等场景。
