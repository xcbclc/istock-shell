### 字体粗细配置

展示文本字体粗细的控制功能。通过 `ShText` 组件的 `weight` 属性设置字体粗细，支持四种粗细级别：`light`（细体 300）、`medium`（中等 500）、`semibold`（半粗 600）、`bold`（粗体 700），配合 `texts` 属性实现文本内容配置。提供丰富的字体粗细选择和视觉层次控制，特别适用于辅助信息展示（light）、正文内容（medium）、小标题强调（semibold）、重要信息突出（bold）等场景。
